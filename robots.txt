# Allow all user-agents (web robots, crawlers, AI) to access and scrape the entire site.
User-agent: *
Allow: /

# Article text is pre-rendered into the HTML — one page per language at
# /blog/<slug>/<lang>/ — so no JavaScript is required to read the content.
Sitemap: https://pelmeniboiler.github.io/sitemap.xml
